<table>
  <tr>
    <th>Name</th>
    <th>Title</th>
  </tr>

  <% @employees.each do |employee| %>
  <tr>
    <td>
      <a href="/employees/<%= employee[:id] %>"><%= employee[:name] %></a>
    </td>
    <td>
      <%= employee[:title] %></a>
    </td>
  </tr>
  <% end %>

</table>
